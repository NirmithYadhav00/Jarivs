name: frontend
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  speech_to_text: ^7.3.0
  flutter_tts: ^3.8.5
  http: ^1.2.0
  url_launcher: ^6.2.5
  flutter_riverpod: ^2.5.1
  permission_handler: ^11.3.1
  device_apps: ^2.2.0
  android_intent_plus: ^4.0.0   # ✅ FIXED POSITION

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true